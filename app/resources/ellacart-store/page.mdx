import Container from "@/components/Container";
import ButtonGroup from "@/components/mdxComponents/button-group"
import ImageComponent from "@/components/mdxComponents/image-component"
import { DollarSign, HeartHandshake, PackageOpen, RefreshCw, Search, Trees } from "lucide-react";
import Input from "@/components/Input"
import Card from "@/components/Card"
import Store from "@/public/store.png"

export const metadata = {
  title: "Ellacart store"
}

export const susData = [
  [DollarSign, "Flexible finance", "Latest offers, flexible payment options and no-cost EMIs with available at our stores.", "danger", [
    {
      text: "Learn more",
      uri: "/resources/financing",
      variant: ""
    },
  ]],
  [HeartHandshake, "Ellacart specialists", "Our in-store specialists help you choose the right device, with in-store demonstrations.", "success"],
  [RefreshCw, "Exchange program", "Exchange your old device for the device you want and earn cashback when you exchange your old phone.", "warning", [
    {
      text: "Learn more",
      uri: "/resources/exchange-program",
      variant: ""
    },
  ]]
]

<div className="py-24">
  <Container>
  <div className="flex flex-col gap-16 lg:flex-row items-center">
      <div className="flex flex-col items-center gap-6 flex-1">
      # Take a trip to our Ellacart Store.
      #### Shop for devices, one on one with a specialist.
  
      <ButtonGroup
        data={
          [
            ["Locate a store", "", "", "#stores", ""],
            ["Shop online", "", "bordered", "/shop", ""]
          ]
        }
      />
      </div>
      <ImageComponent src={Store} pos="bottom" />
    </div>
  </Container>

  <div className="mt-32 bg-zinc-50 py-24" id="stores">
    <Container>
      ## Locate a store
      <div className="mt-16 max-w-xl mx-auto">
        <Input placeholder="Search for a store" icon={Search} type="text" large />
      </div>
    </Container>
  </div>

  <Container>
    <div className="mt-32 flex flex-col gap-6">
      
        ## Same seamless experience even at stores.

        <div className="mt-16 grid grid-cols-1 lg:grid-cols-3 gap-6">
          {susData.map(([icon, title, desc, accent, buttons], index) => (
            <Card title={title} desc={desc} icon={icon} accent={accent} buttons={buttons} key={index} />
          ))}
        </div>

    </div>
  </Container>
# Exchange for the device you want.
  <Container>
    <div className="mt-64 flex flex-col items-center gap-6">
      
      ## Get your device repaired at Ellacart&trade; Store.

      #### Start a repair request below to make a reservation with a repair specialist to get help with a hardware repair.

      <ButtonGroup
      data={
        [
          ["Visit a store", "", "", "#stores", ""],
          ["Schedule pickup", "", "bordered", "/services/repair/schedule  ", ""]
        ]
      }
      />        

    </div>
  </Container>

</div>